# ── UI framework ──────────────────────────────────────────────────────────────
customtkinter>=5.2.0

# ── Image processing ──────────────────────────────────────────────────────────
Pillow>=10.0.0

# ── Drag & drop (optional, graceful fallback if not installed) ────────────────
tkinterdnd2>=0.3.0

# ── PDF operations ────────────────────────────────────────────────────────────
pypdf>=4.0.0          # merge, split, encrypt, form fields
reportlab>=4.0.0      # create PDF from images (no OCR path)

# ── PDF visual editor — render pages as images for the Modifica tab ──────────
pymupdf>=1.23.0        # pip install pymupdf  (also known as fitz)

# ── PDF text extraction (optional, better layout than pypdf) ──────────────────
pdfplumber>=0.10.0

# ── OCR — searchable PDF (optional, also needs Tesseract binary installed) ────
# pytesseract>=0.3.10

# ── Audio Manager ─────────────────────────────────────────────────────────────
pydub>=0.25.1           # audio manipulation
imageio-ffmpeg>=0.4.9   # ffmpeg bundled — nessuna installazione manuale necessaria
soundfile>=0.12.1       # lossless audio read/write (WAV, FLAC, OGG)
noisereduce>=3.0.0      # spectral gating noise reduction
numpy>=1.26.0           # array operations (shared dep)
scipy>=1.12.0           # Butterworth EQ filters
mutagen>=1.47.0         # ID3 / Vorbis / MP4 / FLAC tag read+write

# ── Stem separation (optional — ~2 GB, requires PyTorch first) ────────────────
# pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
# demucs>=4.0.0
