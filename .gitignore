__pycache__/
*.pyc
*.pyo
*.pyd

# PyInstaller build output
/build/
/dist/
*.spec.bak

# Vendored native OCR binaries — large, per-build-machine binaries the
# developer copies in once before packaging (see vendor/tesseract/README.md).
# Never commit the actual binaries to git.
/vendor/tesseract/*
!/vendor/tesseract/README.md

